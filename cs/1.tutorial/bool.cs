using System;

public class Bool
{
  public static void Main(string[] args)
  {
    // true and false with bools
    /*
    bool isWoman = true;
    bool isCool = false;
    Console.WriteLine(isWoman + "\n"); // outputs True
    Console.WriteLine(isCool); // outputs False
    */
    // comparisons
    int x = 10, y = 9;
    Console.WriteLine(x > y);
    Console.WriteLine(x == 10);
  }
}
